# Project Configuration & Development Standards (LiteSql)

Tên dự án hiện tại: **LiteSql**

---

## 1. Giới thiệu & Mục tiêu
`LiteSql` là một thư viện ORM siêu nhẹ, hiệu năng cao thay thế cho LINQ to SQL (L2S) dựa trên nền tảng Dapper, hỗ trợ tốt .NET Standard 2.0.

Các tính năng cốt lõi:
- **Attribute Mapping:** Ánh xạ thực thể với cơ sở dữ liệu thông qua các attribute hoặc quy ước đặt tên (convention mapping).
- **LiteSql.CodeGen CLI:** Công cụ sinh mã nguồn tự động hỗ trợ chuyển đổi từ file `.dbml` (LINQ to SQL) sang mô hình LiteSql.
- **WhereBuilder:** Dịch biểu thức C# Lambda (Expressions) thành câu lệnh SQL WHERE thuần túy an toàn.
- **Theo dõi thay đổi (Change Tracking):** Sử dụng cơ chế Snapshot để chỉ cập nhật những cột thực sự thay đổi khi gọi `SubmitChanges()`.
- **Hỗ trợ đa Database:** Hoạt động tốt với SQL Server và SQLite.
- **Async API & EF Features:** Tích hợp đầy đủ API bất đồng bộ (`SubmitChangesAsync`, `ExecuteQueryAsync`) và các tính năng như `AsNoTracking()`, `Find/FindAsync`.

---

## 2. Quy chuẩn Phát triển & Kiến trúc
- **Tương thích L2S:** Giữ vững khả năng tương thích ngược để dễ dàng port code cũ từ LINQ to SQL sang LiteSql.
- **Quản lý Snapshot:** Sử dụng `Attach` và `Detach` một cách rõ ràng khi cần theo dõi hoặc bỏ theo dõi các thay đổi của thực thể.
- **Dapper Integration:** Tận dụng tối đa tốc độ của Dapper cho các truy vấn raw SQL hoặc các thủ tục lưu sẵn (Stored Procedures).

---

## 3. Lệnh Hữu ích
*   **Build dự án:**
    ```bash
    dotnet build src/LiteSql/LiteSql.csproj -c Release
    ```
*   **Chạy Unit/Integration Tests:**
    ```bash
    dotnet test tests/LiteSql.Tests/LiteSql.Tests.csproj
    ```
